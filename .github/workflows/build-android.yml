# Unity CI has persistent license activation issues with GameCI
# FREE ALTERNATIVES:

# 1. LOCAL BUILDS (Recommended for learning):
#    - Unity Editor > File > Build Settings > Android > Build
#    - Upload APK to GitHub Releases manually

# 2. GITLAB CI (Free tier includes CI/CD):
#    - GitLab has better Unity license handling
#    - 400 minutes/month free CI time

# 3. CIRCLECI (Free tier):
#    - 6000 build minutes/month free
#    - Better Unity container support

# 4. SELF-HOSTED RUNNER:
#    - Use your own PC as GitHub Actions runner
#    - Unlimited builds, no license issues

name: Build Android APK - DISABLED

# Disabled due to Unity license activation issues in CI
# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]

jobs:
  build:
    name: Build Android
    runs-on: ubuntu-latest
    if: false  # Disabled
    
    steps:
    - name: Placeholder
      run: echo "Use local builds or migrate to GitLab CI for free Unity builds"